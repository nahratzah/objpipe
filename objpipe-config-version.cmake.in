set(PACKAGE_VERSION @OBJPIPE_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@OBJPIPE_VERSION_MAJOR@")
  if ("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@OBJPIPE_VERSION_MINOR@")
    set(PACKAGE_VERSION_EXACT TRUE)
  elseif("${PACKAGE_FIND_VERSION_MINOR}" LESS "@OBJPIPE_VERSION_MINOR@")
    set(PACKAGE_VERSION_COMPATIBLE TRUE)
  else()
    set(PACKAGE_VERSION_UNSUITABLE TRUE)
  endif()
else()
  set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
